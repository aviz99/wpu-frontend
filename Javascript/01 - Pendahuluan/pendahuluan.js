// Tipe Data di Javascript
// number,string,boolean,object,function,undefined

// di Javascript tidak ada integer melainkan bilangan floating point atau bisa disebut dengan bilangan pecahan

// Ukuran yang bisa ditampung dijavascript adalah 64bit atau bisa disebut dengan 2 pangkat 64bit

// Operator pada javascript adalah sebuah simbol yang digunakan untuk melakukan operasi
// Jenis jenis operator pada javascript
// aritmatika, penugasan, perbandingan, logika, string, kondisional, typeof
// operator aritmatika, penugasan, perbandingan, logika, string disebut dengan binary
// sedangkan operator kondisional disebut dengan ternary dan operator typeof disebut dengan unary

// operator binary membutuhkan 2 operand
// contoh : operand1 operator operand2

// operator unary membutuhkan 1 operand
// contoh : operator operand atau operand operator

// operator ternary membutuhkan 3 operand

// operator aritmatika
// +, -, *, /, %

// operator presedence atau urutan operasi matematika
// () * / + -

// operator penugasan / operator assingment
// =, +=, -=, *=, /=, %=

// operator pembanding / operator comparison
// == (sama dengan), >= (lebih besar sama dengan), <= (lebih kecil sama dengan), != (tidak sama dengan), === (strict (operator identitas) sama dengan), !== (strict tidak sama dengan), > (lebih dari), < (kurang dari)
// dapat menghasilkan nilai boolean (true atau false)

// operator logika
// operator ini digunakan untuk menentukan logika dari beberapa ekspresi yang kalian gabungkan
// contoh : && (AND) keduanya harus bernilai true, || (OR) salah satu nya bernilai true, ! (NOT) membalikkan hasil boolean nya (jika true maka akan dibalik jadi false begitu juga sebaliknya).

// operator string
// operator penggabung antara 2 buah string
// contoh : +

// operator typeof
// operator yang mengidentifikasi tipe data apa yang dimasukkan
// contoh : typeof(operand)

// operator kondisional
// jenis operator ternary dimana kita harus membutuhkan 3 operand didalamnya operator ini digunakan untuk melakukan pengecekan suatu keadaan dan menentukan sebuah nilai yang dihasilkan ketika kondisi nya bernilai true atau false
// contoh : (kondisi) ? benar : salah

// escape character
// contoh : \' atau \", \0 (karakter NULL), \\ (\), \n (baris baru atau new line), \t (tab), \b (backspace), \uxxxx (unicode)
// .length berfungsi untuk menghitung panjang dari sebuah string

// function boolean
// boolean(nilai)

// nilai truthy
// true, non-zero number, 'string', arrays, functions

// nilai falsy
// false, 0, '', undefined (sebuah variabel yang dibuat atau dideklarasikan tetapi kita belum membuat atau menmapung nilai ke variabel tersebut), null(nilai kosong yang kita bisa berikan sebuah variabel), NaN

// variabel adalah sebuah tempat atau penampung sebuah nilai yang digunakan untuk menampung sebuah nilai

// deklarasi adalah mendaftarkan sebuah variabel ke dalam lingkup yang sesuai
// inisialisasi adalah proses suatu pemesanan atau penyediaan memori untuk variabel
// assingment adalah untuk menetapkan nilai yang spesifik untuk disimpan kedalam variabel
// contoh : var x; (deklarasi & inisialisasi)
// contoh : x = 20; (assingment)
// jenis deklarasi variabel
// var, let, const

alert('hello\nworld');
console.log('Hello World!');